# O que NAO entra no contexto do build.
#
# Alem de deixar o build mais rapido, isto e protecao: sem a linha do `.env`,
# um arquivo de credenciais local seria copiado para dentro da imagem pelo
# `COPY . .` do estagio de build.

.env
.env.*
!.env.example

node_modules
.next
out
build

.git
.github
.claude

# Testes e ferramentas de desenvolvimento nao rodam em producao.
tests
**/*.test.ts
**/*.test.tsx
config/vitest.config.ts
config/vitest.setup.ts
coverage

docs
templates
*.md
!README.md

docker-compose.yml
Dockerfile
.dockerignore

.vscode
.idea
*.log
